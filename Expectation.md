# Expectation

- Visual prioritization of urgency and importance
  - With a coordinate system in the graph or a single one row with multiple column

- Each event has it's own type (event, homework etc.)

- Allow user to use LLM to add the new event.

- Offer API for event list, user has it's own token

- Support Local database or online database with postgreph.
  - Use supabase for remote database

- Support multiple user login, support local login and Auth0
